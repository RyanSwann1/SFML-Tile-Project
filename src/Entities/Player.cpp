#include "Entities\Player.h"

Player::Player(const SharedContext & sharedContext, const std::string player)
	: Character(sharedContext, player)
{

}

void Player::update(const float deltaTime)
{
}
